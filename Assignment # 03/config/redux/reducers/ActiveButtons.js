const INITIAL_STATE = {
    type:'ACTIVEBUTTONS',
    categoryButton:'All',
    bottomMenu:'Home'

    
};

const ActiveButtons = (state = INITIAL_STATE, action) => state

export default ActiveButtons;